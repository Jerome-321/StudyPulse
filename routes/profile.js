// src/routes/profile.js
import { Router } from "express";
const router = Router();

// For this example, your profile routes are handled inline in server.js,
// so just export an empty router here to satisfy the import.
export default router;
